import { styled, lightTheme } from "styles/stitches.config";

export const HeadlineSubSection = styled("div", {
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  justifyContent: "center",
  textAlign: "center",
  gap: "3rem",
});
